# 사이트 설정 — 값은 여기서만 바꾼다. 개인 주소는 넣지 않는다(동 단위 대표 좌표만).
title: "모닝브리프"
timezone: "Asia/Seoul"

locations:            # 표시 이름만. 실제 주소 없음 (결정 ② 2026-09-19)
  - name: "집"
    lat: 37.32
    lon: 126.82

tabs:                 # 화면 탭 순서 · 표시 여부. module = sources.yaml 의 modules 키
  - {module: news,       label: "뉴스"}
  - {module: weather,    label: "날씨"}
  - {module: markets,    label: "지수"}
  - {module: fx,         label: "환율"}
  - {module: crypto,     label: "코인"}
  - {module: realestate, label: "부동산"}
  - {module: it,         label: "IT·가전"}
  - {module: auto,       label: "자동차"}
  - {module: leisure,    label: "레저·캠핑"}
  - {module: publicdata, label: "공공데이터"}

history_days: 90      # 이력 보존 (반복 판정 재료)
must_know_count: 5    # 반드시 알아야 할 것 건수
